define([
    "dojo/_base/declare",
    "./BaseRasterFunctionWidget"
],
    function (declare, BaseRasterFunctionWidget) {
        return declare(
            [BaseRasterFunctionWidget],
            {
                label: "Aspect",
                functionName: "Aspect"

            });

    });